Make the following changes:

Update Intel:
    1. Correctly define XP:
        Update the prompt with correct defitiion to how to giev xp to each area, based on psycjhology research
        Get output xp count as a vlaue between 0-1, then use it to get exfact xp count:
            1. It should be proportional to lifetime avg. 
            2. should be multiplied by hours passed after last reflection

    2. Update startup screen:
        1. WHen startup is clicked (from now on) save the progress of current tasks into database (all of them, submissions) and then when I create the breifing, show a view with how much incriment we had on each tasks, sort them based on scehdule and the design must contain a progress bar
        2. On breifing report, remove the red asstets screen, replace it by ten things we are grateful today, with green color advanced view connecting to intel screen theme, also get an icon for each of these card of gratitude
        3. Update week reports based on new changes, so it must contain gratitude and overall four agent progress throught the week

    3. Finance report:
        Include a fiance summary in weeky report
    
    4. Repalce the budget progressbar with a bar of income vs expanse
    5. Show a crd which shows projected income of month


Make sure there wont be any screensize error, our ideal screen is 720x1520 with 271 dpi
When adding a new ui, create very compact design based on the theme of jurassic world evolution

After applying the changes recreate project_snapshot.txt only for new files and files with changes, each time improve modularity of the program by introducing new component files (only on modified or new files), don't modify system files like pubspec, if we need new packages, or file path changes, removal etc, give the command for bash


Thoroughly check for ui sizing errors before writing the code
Note: as output only give project_snapshot and commands if needed
