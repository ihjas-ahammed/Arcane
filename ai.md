Make the following changes:

     - Add projects tab:
        - Move settings to top right (along with persona virtues icon)
        - Ewplace settings with a projects tab
          - Add project window as follows:

          It will have a name
it will have a description 

it will have steps
And steps will have substeps 

All of the above can have description and substeps (to infinity)

There will be a progress bar (rounded)
For projects, substeps the same way

You can create project from ai prompts 

You can add a project/substep as a submission in any of the missions

- Each mission will have seperate set of projects


After applying the changes recreate project_snapshot.txt only for new files and files with changes, each time improve modularity of the program by introducing new component files (only on modified or new files), don't modify system files like pubspec, if we need new packages, or file path cahnges, removal etc, give the command for bash


Throughly check for ui sizing errors before writing the code